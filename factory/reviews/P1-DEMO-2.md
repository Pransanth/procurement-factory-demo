# P1-DEMO-2

Finding: P1-DEMO-2
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: a856f21ea34ef0df2
Reviewed Commit: 0059665d19b8ee3bc084ec71c2b4a00e69fa1810 on branch fix/P1-DEMO-2 (base main was ef24ec0e1a399cad87d06941a46ab69751c2acad; PR #6 at https://github.com/Pransanth/procurement-factory-demo/pull/6)
Result: PASS
Root Cause Addressed: Yes. Finding's Analyse states create() never checks supplier_id belongs to organization_id. app/repositories/procurement_requests.py:38-41 now calls suppliers.get_by_id(conn, organization_id, supplier_id) before the INSERT (line 44) and raises ValueError if None, mirroring app/repositories/approvals.py:record_decision (lines 33-42) as the build order specified as the primary security boundary. This closes the only production call path since app/services/procurement_service.py:create_request (line 13) calls procurement_requests.create() directly with no bypass route.
Regression Evidence Checked: Read app/repositories/test_supplier_org_scope_regression.py in full. Its assertion (assertRaises(ValueError) around procurement_requests.create() with a cross-org supplier_id, plus assertEqual(list_for_org(...), []) confirming no row inserted) matches exactly what the build order's Red Regression Evidence (AssertionError: ValueError not raised) and Green Runtime Fix Evidence sections claim was proven red-then-green; the code is internally consistent with that narrative. Could not execute the test myself (no Bash tool) to independently reproduce red/green — this is a stated limitation, not a finding of fabrication.
Guard Evidence Checked: Read factory/guards/run-app-tests.py in full: discover_test_modules() (line 27-32) does APP_DIR.rglob("test_*.py"), genuinely auto-discovering the new test file with no manual registration, confirming the Central Guard Plan's mechanism. Read .github/workflows/factory-ci.yml: line 34-35 runs "python3 factory/guards/run-app-tests.py" as its own CI step on every push/PR to main (lines 7-11), independent of run-factory-checks.py (which only validates findings/job-handler-scope/reviews per factory/guards/run-factory-checks.py lines 9-26, and does not itself run app tests). I agree the reasoning in the Central Guard Plan is sound for a runtime data-validation gap of this shape (single repository function, no structural API redesign) and that it was actually implemented as claimed, not just asserted.
Scope Checked: Changed files exactly match build order's Scope section: app/repositories/procurement_requests.py (new suppliers import + validation in create()) and new app/repositories/test_supplier_org_scope_regression.py. Confirmed via Grep that no app/jobs/* file calls procurement_requests.create(), consistent with "Nicht erlaubt: Keine Änderung an ... app/jobs/*" and the finding's claim that background jobs are unaffected. No changes to app/db.py, approvals.py, or requested_by_user_id handling, matching the explicit out-of-scope list.
Remaining Risks: None identified beyond the inherent limitation that this review is static/read-only and cannot itself execute the regression suite or reproduce the cited CI run (https://github.com/Pransanth/procurement-factory-demo/actions/runs/31802524965/job/94773620222) to confirm it is genuine and green; the quoted evidence is internally consistent with current source but was not independently re-run.
Findings And Objections: None.
