# P1-DEMO-5

Finding: P1-DEMO-5
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: ad8a8e78f5c60035e
Reviewed Commit: 36b2362 (branch fix/P1-DEMO-5, PR #14 against main; branch base is origin/main 21b78bbc3a5cc2c4f2ba4c61a9b7cb90bce5a554, which already contains the closed finding P1-DEMO-4)
Result: FAIL
Root Cause Addressed: Yes, at the documented location and unchanged since round 1. /Users/clauskopp/Downloads/procurement-factory-demo/app/services/user_admin_service.py:20 defines ROLE_ADMINISTERING_ROLES = ("admin",) and line 39 reads "if actor.role not in ROLE_ADMINISTERING_ROLES:", replacing the exclusion test named in the finding's Root Cause ("if actor.role == 'member': raise"). The check sits after the actor is resolved (line 35) and before the target load (line 45), the write (users.update_role, line 49) and the audit entry (lines 50-58), so a rejected call mutates nothing and records nothing. I re-verified there is no second write path: grep for update_role across the repo shows the only production caller is user_admin_service.py:49 (app/repositories/users.py:55 is the storage function; the other hits are tests and guard fixtures). The error message at lines 41-42 still names actor.role, preserving diagnostics. I again independently agree with the deliberate omission of a self-change ban: ASSIGNABLE_ROLES (line 12) contains no role above 'admin', so with an admin-only allowlist a self-change can only reduce privilege.
Regression Evidence Checked: Cross-checked the build order's red output (factory/build-orders/P1-DEMO-5.md:112-123) against the current /Users/clauskopp/Downloads/procurement-factory-demo/app/services/test_user_admin_role_escalation_regression.py. All four originally quoted test names still exist verbatim (lines 41, 50, 57, 64) and none was renamed or removed. The round-1 objection is genuinely fixed and not merely restyled: NOT_AUTHORIZED = "not authorized to administer user roles" (line 39) is used via assertRaisesRegex at lines 42, 51, 58, and that phrase occurs in the module only in the authorization branch (user_admin_service.py:41-42, string concatenation yields "... is not authorized to administer user roles"); the three other reachable ValueError messages ("role '...' is not assignable", "actor ... not found in organization", "user ... not found in organization") do not contain it, and the pattern has no regex metacharacters, so re.search cannot mask a different failure. The state assertions from round 1 are all still present and unweakened (stored role unchanged, lines 46-48 and 55; audit_log.list_for_org(...) == [] at line 62). I re-verified that empty-audit assertion is non-vacuous: app/repositories/users.py:29-37 (create) writes no audit row and only app/repositories/audit_log.py:31-50 (record) inserts. The new test_admin_may_still_demote_themselves (lines 73-84) asserts something real, not a tautology: it asserts both updated.role == "member" and the persisted row via users.get_by_id, so a future self-change ban would make it fail loudly. It is a pinning test, not a red-then-green one (it would have passed pre-fix as well), which the build order states honestly. App-test count cross-check: I counted 77 test methods across app/ (19 files), exactly matching the claimed "run-app-tests.py -- 77 Tests OK", and 5 in the regression file matching "5 Tests OK". No pre-existing test was touched: app/services/test_user_admin_service.py still contains all six original cases with 'admin'/'member' actors only. I cannot execute anything, so the red-before/green-after transitions themselves remain claims corroborated only by file content and internal consistency.
Guard Evidence Checked: Read factory/guards/validate-service-role-authorization.py in full and re-traced check_comparison (lines 90-120) by hand against all 12 fixtures. Normal form, reversed operands (elif line 104), plain role variable (ROLE_NAMES line 63), and "!=" (ast.NotEq accepted at line 96, symbol rendered line 108) are all flagged with messages that match the assertions at test lines 133, 145, 154, 159. Membership forms are neither Eq nor NotEq and are skipped at lines 96-98, so both the fixed module and a literal-tuple allowlist pass; reading actor.role inside an f-string is a FormattedValue, not a Compare, so user_admin_service.py:41 is not self-flagged (pinned by test_role_mentioned_only_inside_a_message_is_accepted). The two new known-limitation fixtures are honest, not a blessing dressed as documentation: DENYLIST_MEMBERSHIP_CHECK (test lines 84-92) and COMPARISON_AGAINST_A_NAMED_CONSTANT (lines 94-102) assert returncode 0 with comments naming them as recorded gaps, and the guard docstring lines 42-50 now names both forms explicitly ("a comparison against a NAMED constant ... and an exclusion expressed as a membership test over a denylist"). The finding's rewritten Central Guard Plan (factory/findings/P1-DEMO-5.md:57) now matches the implementation: "strictly the literal-comparison form it matches, and no more. Two rewrites of the same decision pass it and are named as known limitations" -- the round-1 "unwritable" overstatement is gone. Guard test count is 12 (verified by counting methods), runner test count is 23 (verified: 19 pre-existing all still present verbatim plus 4 new at lines 348-377), matching the build order. Runner wiring re-verified: SERVICE_ROLE_GUARD (run-factory-checks.py:62), run_service_role_checks (lines 248-294), invoked at line 329, ANDed at line 331, report appended at line 332 -- it cannot pass silently. False-positive risk against real code: grep across app/ for role equality/inequality comparisons returns no matches, so the claimed "[OK] service-role-guard" for all three service modules is consistent with actual file contents. Numeric defect found here: the finding's Verification Evidence item (6) claims "die sechs Guard-Testmodule zusammen -- 68 Tests OK" in the same sentence that correctly claims 12 for the role guard. 68 was round 1's figure with 10 role tests; the same six modules today total 70 (14 + 7 + 9 + 6 + 22 + 12), and my count of the other five (58) exactly reproduces round 1's observed 68 - 10, which validates the counting method. No other of the six modules was modified in this commit (mtime ordering), so the number is stale, internally contradictory, and cannot be the result of the "frisch ausgefuehrt" run it is presented as.
Scope Checked: The set of files with the newest modification times (Glob over the whole tree, ascending) is exactly: app/services/user_admin_service.py, factory/guards/run-factory-checks.py, factory/guards/test_run_factory_checks.py, factory/reviews/P1-DEMO-5.md, factory/guards/validate-service-role-authorization.py, factory/guards/test_validate_service_role_authorization.py, app/services/test_user_admin_role_escalation_regression.py, factory/build-orders/P1-DEMO-5.md, factory/findings/P1-DEMO-5.md. That matches the build order's allowed list (factory/build-orders/P1-DEMO-5.md:66-74) plus the finding, the build order and the hook-written review artifact, none of which are on the forbidden list. All forbidden files are untouched and older: app/repositories/users.py (update_role unchanged at lines 55-63), app/repositories/approvals.py, app/db.py, app/services/reporting_service.py, .github/workflows/factory-ci.yml, CLAUDE.md, .claude/settings.json and .claude/hooks/. Caveat: without Bash I cannot run git diff, so scope conformity rests on file content plus mtime ordering, not on the diff itself.
Remaining Risks: (1) The finding's CI Evidence documents the green Actions run 31811498023 for the superseded commit 0df18bc, not for the reviewed commit 36b2362, whose guard fixtures and regression file differ; the in-flight run for 36b2362 cannot be confirmed with read-only tools and must be recorded before READY_FOR_CLOSURE. (2) The finding still has no "Review Artifact:" field at all (factory/findings/P1-DEMO-5.md:60-65 go straight from CI Evidence to Expert Review Reason), which validate-finding.py requires for READY_FOR_CLOSURE. (3) The two known-limitation fixtures pin returncode 0, so a future tightening of the guard to catch denylist membership or named constants will require editing these tests -- acceptable and clearly named, but it does convert a gap into asserted behaviour. (4) The guard covers only app/services/ non-recursively; the same class of decision in app/repositories/approvals.py:43 would go undetected if rewritten as an exclusion. (5) app/repositories/users.py:55 update_role remains unauthenticated, so the boundary is a single service function rather than a choke point. (6) test_admin_may_still_demote_themselves now pins a path by which the last admin demotes themselves to 'member', after which no role can administer roles and the organization cannot recover through this API -- deliberate and out of scope, but it is now an asserted behaviour rather than an unexamined one. (7) All quoted command outputs and the red/green transitions can only be confirmed by a live run, which I cannot perform.
Findings And Objections: One blocking objection, concrete and narrow. factory/findings/P1-DEMO-5.md:60 (Verification Evidence, item 6) states, as part of a run explicitly dated and described as "Am 2026-08-14 ... frisch ausgefuehrt", that "die sechs Guard-Testmodule zusammen -- 68 Tests OK". That number cannot be true of this commit: the same sentence's item (3) correctly records that factory.guards.test_validate_service_role_authorization now has 12 tests (10 from round 1 plus the two new known-limitation fixtures at test file lines 176-188), and that module is one of the six. Counting test methods gives 14 (test_validate_finding) + 7 (test_validate_job_handler_scope) + 9 (test_validate_review) + 6 (test_create_finding_worktree) + 22 (test_validate_service_sql_org_scope) + 12 = 70, and the other five sum to 58, which is exactly round 1's observed 68 minus the then-10 role tests -- so the figure was carried forward from round 1 rather than re-measured, in the one field the workflow requires to name concretely which tests ran green. That is a misstatement of verification evidence, self-contradictory within a single sentence, and it must be corrected from a real re-run (expected: 70) before closure; the same pass should update CI Evidence to the reviewed commit and add the missing Review Artifact field. Everything else I checked holds: the runtime fix addresses the documented root cause at the right point in the function, the regression assertions were strengthened (assertRaisesRegex pins a message no other ValueError in the module produces) and none was loosened, removed or renamed, the new self-demotion test asserts both the return value and the persisted row, the known-limitation fixtures document rather than conceal the guard's two blind spots and the finding's Central Guard Plan sentence now matches the guard's actual rule, no existing test or control was weakened or skipped (test_user_admin_service.py intact with six cases, test_run_factory_checks.py only extended, no suppressions or disabled checks in any changed file), no new import, parameter or private-attribute exposure was added to the runtime module, and no file outside the build order's Scope was touched.
