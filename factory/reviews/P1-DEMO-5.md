# P1-DEMO-5

Finding: P1-DEMO-5
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: a75ffdcef8be2d131
Reviewed Commit: 7e6dfde (branch fix/P1-DEMO-5, PR #14 against main; branch base is origin/main 21b78bbc3a5cc2c4f2ba4c61a9b7cb90bce5a554, which already contains the closed finding P1-DEMO-4)
Result: PASS
Root Cause Addressed: Yes, unchanged from what round 2 described and verified again from source. /Users/clauskopp/Downloads/procurement-factory-demo/app/services/user_admin_service.py:20 defines ROLE_ADMINISTERING_ROLES = ("admin",) and line 39 is "if actor.role not in ROLE_ADMINISTERING_ROLES:", replacing the exclusion test named in the finding's Root Cause ("if actor.role == 'member': raise"). Placement is correct: actor resolved at line 35, authorization at 39-43, target load at 45, write at 49 (users.update_role), audit entry at 50-58 -- a rejected call writes nothing and logs nothing. Grep across the repo for "update_role" confirms the only production caller is user_admin_service.py:49 (storage function at app/repositories/users.py:55-63, otherwise only tests and guard fixtures), so this is the single decision point for the operation. app/repositories/users.py is byte-for-byte the org-scoped storage layer the build order says must not change (lines 55-63 still unauthenticated, org-filtered UPDATE). The deliberate omission of a self-change ban remains sound: ASSIGNABLE_ROLES (line 12) has no role above 'admin', so with an admin-only allowlist a self-change can only reduce privilege. Error message (lines 41-42) still names actor.role.
Regression Evidence Checked: I cross-checked the build order's Red Regression Evidence (factory/build-orders/P1-DEMO-5.md:109-129) against the current /Users/clauskopp/Downloads/procurement-factory-demo/app/services/test_user_admin_role_escalation_regression.py: all four originally-red/green-quoted test names still exist verbatim (lines 41, 50, 57, 64), none renamed or removed, plus the round-1 addition test_admin_may_still_demote_themselves (line 73). Assertions are strictly stronger than the quoted red run, never weaker: NOT_AUTHORIZED = "not authorized to administer user roles" (line 39) is enforced via assertRaisesRegex at lines 42, 51, 58; I confirmed that phrase is produced only by the authorization branch (user_admin_service.py:41-42), while the three other reachable ValueErrors ("role '...' is not assignable" line 32, "actor ... not found" line 37, "user ... not found" line 47) do not contain it, and the pattern has no regex metacharacters. State assertions intact: stored role unchanged (lines 46-48, 55, 70-71, 84) and audit_log.list_for_org(...) == [] (line 62), which is non-vacuous because app/repositories/users.py:29-37 writes no audit row. The pre-existing baseline app/services/test_user_admin_service.py is untouched and still holds six cases (lines 34, 43, 52, 59, 66, 73) with only 'admin'/'member' actors, so the tightening cannot have been absorbed by weakening it. Counts I re-derived myself rather than trusting the text: 5 test methods in the regression file; 77 test methods across 19 files under app/ (matches "77 Tests OK (72 bestehende + 5 neue)", 77-5=72); 6 (test_user_admin_service) + 7 (app/repositories/test_users.py) + 6 (app/test_multi_tenant_isolation.py) = 19, matching the claimed 19. factory/guards/run-app-tests.py:29 does rglob("test_*.py") over app/, so its total legitimately equals my 77. I cannot execute anything, so the red-before / green-after transition itself remains a documented claim corroborated only by file content and internal consistency.
Guard Evidence Checked: The round-2 FAIL was numeric, and I re-measured the disputed figure independently instead of trusting the correction. Counting test methods gives test_validate_finding 14 (two TestCase classes, 6+8, no inheritance or load_tests anywhere in factory/guards), test_validate_job_handler_scope 7, test_validate_review 9, test_create_finding_worktree 6, test_validate_service_sql_org_scope 22, test_validate_service_role_authorization 12 -- sum exactly 70, matching factory/findings/P1-DEMO-5.md:60's new "70 Tests OK ... (14 + 7 + 9 + 6 + 22 + 12)". The stale 68 is gone and its rejection is recorded rather than erased (finding line 60; build order lines 239-259). I also checked the round-1 evidence block that still says 68 (build order line 179) is not now-false-in-place: the other five modules total 58 and the role guard had 10 tests in round 1 (12 today minus the two known-limitation fixtures at test lines 176-188), so 58+10=68 is a correct historical record for that commit, and the build order's round-2 section explicitly supersedes it. Every other figure in Verification Evidence re-derived and correct: test_run_factory_checks 23 (19 pre-existing plus 4 new at lines 348-377), .claude/hooks/test_stop_validate_findings.py 5, .claude/hooks/test_subagentstop_write_review.py 13, three service modules under app/services/. Guard logic re-traced by hand: check_comparison (validate-service-role-authorization.py:90-120) flags Eq/NotEq where one side is an Attribute ".role" or a Name in {role, actor_role, user_role} and the other a string Constant, in either operand order (lines 102-105), and renders the exact strings the tests assert ("actor.role == 'member'", "actor.role != 'admin'", "role == 'member'" at test lines 133, 145, 159); membership tests are neither Eq nor NotEq and are skipped at lines 96-98, so the fixed module and literal-tuple allowlists pass, and reading actor.role inside an f-string is a FormattedValue rather than a Compare, so the fixed module does not self-flag. The two documented blind spots are pinned honestly as returncode-0 fixtures with explicit comments (test lines 176-188) and named in the docstring (lines 42-50); the finding's Central Guard Plan (line 57) matches that scope word for word ("strictly the literal-comparison form it matches, and no more"). Runner wiring intact: SERVICE_ROLE_GUARD at run-factory-checks.py:62, run_service_role_checks at 248-294, called at 329, ANDed into ok at 331, report merged at 332 -- it cannot pass silently. Grep for role equality/inequality across app/ returns no matches, so the claimed "[OK] service-role-guard" for all three service modules is consistent with actual file content. The committed round-2 FAIL artifact (factory/reviews/P1-DEMO-5.md) is structurally valid against validate-review.py's twelve required fields, VALID_REVIEWER_AGENT_TYPE and ALLOWED_RESULTS, so committing it does not break the canonical runner.
Scope Checked: Glob over the whole tree by modification time puts the three newest files at factory/reviews/P1-DEMO-5.md, factory/findings/P1-DEMO-5.md, factory/build-orders/P1-DEMO-5.md, with app/services/user_admin_service.py, factory/guards/run-factory-checks.py, factory/guards/test_run_factory_checks.py, factory/guards/validate-service-role-authorization.py, factory/guards/test_validate_service_role_authorization.py and app/services/test_user_admin_role_escalation_regression.py older -- consistent with the stated claim that this commit touched only the finding, the build order and the review artifact. Every line number cited by round 2 for the runtime module, regression file, guard, guard tests and runner resolves to identical content today, which independently corroborates that no code changed since the reviewed round-2 state. All files still fall inside the build order's Erlaubt list (factory/build-orders/P1-DEMO-5.md:66-74) plus the workflow documents; nothing on the Nicht-erlaubt list changed: app/repositories/users.py, app/repositories/approvals.py (line 43 still the allowlist form), app/db.py, app/services/reporting_service.py, .github/workflows/factory-ci.yml (unchanged five steps), CLAUDE.md, .claude/hooks/ and .claude/settings.json. Minor observation, not a deviation: the Scope section enumerates only code files and never explicitly names the finding, build order or hook-written review artifact, though the workflow requires updating them and the Nicht-erlaubt list does not cover them. Caveat: without Bash I cannot run git diff, so scope conformity rests on file content plus mtime ordering, not on the diff itself.
Remaining Risks: (1) app/repositories/users.py:55 update_role remains unauthenticated, so the boundary is one service function rather than a chokepoint; a future service or job calling update_role directly would bypass it, and the guard is explicitly blind to code with no role check at all (validate-service-role-authorization.py:35-41). (2) The two known-limitation fixtures assert returncode 0, converting the guard's blind spots (named constant, denylist membership) into asserted behaviour that a future tightening must edit. (3) The guard runs only over app/services/ non-recursively (run-factory-checks.py:272), so the same decision form in app/repositories/approvals.py:43 would go undetected if rewritten as an exclusion. (4) .github/workflows/factory-ci.yml:38 runs only five guard test modules and does not include test_validate_service_role_authorization (nor test_validate_service_sql_org_scope); the new rule reaches CI only indirectly, via run-factory-checks.py over real service files and via test_run_factory_checks. The build order's wording (finding line 57, build order lines 83-86) states exactly this and does not overclaim, and workflow edits were out of scope -- but the guard's own 12 fixtures are not exercised by CI. (5) test_admin_may_still_demote_themselves pins a path where the last admin demotes themselves and the organization can no longer administer roles through this API -- deliberate, out of scope, now asserted rather than unexamined. (6) Nothing here was executed: the red-before/green-after transitions, the quoted runner output, the Actions runs 31811498023 (0df18bc) and 31811982062 (36b2362), and the in-flight run for 7e6dfde itself can only be confirmed by a live run; CI Evidence still cites no green run for the exact commit under review, which is inherent to the closure-commit ordering the finding describes.
Findings And Objections: None. The round-2 defect is genuinely repaired and not merely restated: I counted the six guard test modules myself and got 70 (14+7+9+6+22+12), matching the corrected field, and I confirmed the superseded 68 was arithmetically correct for round 1's ten-fixture state, so the record is consistent rather than retrofitted. No other figure in Verification Evidence is stale: 23, 19, 77 (72+5), 5, 13, 12 and "three service modules" all reproduce from current file content. No test or control was weakened, skipped or deleted -- the baseline app/services/test_user_admin_service.py is intact with six cases, test_run_factory_checks.py was only extended (19 originals all present plus 4 at lines 348-377), the regression assertions were strengthened to assertRaisesRegex and none loosened, and a repo-wide grep of app/ found no skip/expectedFailure/type-ignore/xfail suppressions (the only "noqa" is a pre-existing import comment at app/test_multi_tenant_isolation.py:18). The fix introduces no new import, parameter, private-attribute exposure or alternative write path in the runtime module. The FAIL from round 2 is preserved in the build order (lines 239-282) as the documented reason this round exists, so overwriting the artifact with this verdict does not erase it.
