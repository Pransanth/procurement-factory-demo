# P1-DEMO-5

Finding: P1-DEMO-5
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: a6e9e387f4acec1f8
Reviewed Commit: 0df18bc154e6adc44ecf86d1e76cb686fbf3361b (branch fix/P1-DEMO-5, PR #14 against main; branch base is origin/main 21b78bbc3a5cc2c4f2ba4c61a9b7cb90bce5a554, which already contains the closed finding P1-DEMO-4)
Result: PASS
Root Cause Addressed: Yes, at exactly the documented location. The finding's Root Cause is "authorizes the acting user by exclusion instead of by allowlist ... if actor.role == 'member': raise". /Users/clauskopp/Downloads/procurement-factory-demo/app/services/user_admin_service.py:39 now reads "if actor.role not in ROLE_ADMINISTERING_ROLES:" with the allowlist constant ROLE_ADMINISTERING_ROLES = ("admin",) at line 20, documented in the comment at lines 14-19. The check sits after the actor is resolved (line 35), before the target is loaded (line 45) and before both the write (users.update_role, line 49) and the audit entry (line 50) -- so a rejected call performs no write and produces no audit record, which is what makes the third regression assertion meaningful. 'approver' is now denied; the error message still names actor.role (line 41), so the diagnostic value is preserved. I independently agree with the deliberate omission of a self-role-change ban: with an admin-only allowlist the maximum privilege is 'admin', so a self-change can only be a demotion (ASSIGNABLE_ROLES, line 12, contains no role above 'admin'), and banning it would remove the only path for an admin to reduce their own privilege. That is materially different from P1-DEMO-3, where deciding one's own request is an integrity defect independent of role -- the reasoning in factory/build-orders/P1-DEMO-5.md:25-30 holds. The allowlist form also matches the already-correct sibling decision at /Users/clauskopp/Downloads/procurement-factory-demo/app/repositories/approvals.py:43 ("if approver.role not in ('approver', 'admin')").
Regression Evidence Checked: Cross-checked the build order's quoted red output (factory/build-orders/P1-DEMO-5.md:112-123) against the current content of /Users/clauskopp/Downloads/procurement-factory-demo/app/services/test_user_admin_role_escalation_regression.py. All four quoted test names exist verbatim (lines 35, 44, 51, 58) and the quoted output order is the alphabetical order unittest would produce, which is consistent with a real run rather than a reconstruction. The three cases quoted as FAIL are precisely the three that depend on the fix; the fourth (test_admin_can_still_promote_a_member, line 58) is admin-actor and would indeed have been green pre-fix, satisfying acceptance criterion 3. Assertions are not weakened and are stronger than the exception alone: each failure case also pins the persisted state -- role unchanged at lines 40-42 and 49, and audit_log.list_for_org(...) == [] at line 56. I verified that assertion is non-vacuous: users.create (/Users/clauskopp/Downloads/procurement-factory-demo/app/repositories/users.py:29-37) writes no audit row and only audit_log.record inserts (/Users/clauskopp/Downloads/procurement-factory-demo/app/repositories/audit_log.py:31-50), so pre-fix the successful escalation would have left exactly one "user.role_changed" row. The positive case asserts both the returned object and the stored row (lines 62-65), so a fix that denies everything cannot pass. One honest weakness, not a blocker: the three failure cases use assertRaises(ValueError) without matching the message; I checked that no other ValueError is reachable for those inputs ("admin" is in ASSIGNABLE_ROLES, actor and target exist in the org), so the exception can only come from line 40, but assertRaisesRegex would have pinned that structurally. No pre-existing test file was modified: /Users/clauskopp/Downloads/procurement-factory-demo/app/services/test_user_admin_service.py still contains all six original cases and uses only 'admin' and 'member' actors, so the allowlist changes none of its outcomes. I could not re-run any test; the red/green transitions themselves remain claims corroborated only by file content and internal consistency.
Guard Evidence Checked: Read /Users/clauskopp/Downloads/procurement-factory-demo/factory/guards/validate-service-role-authorization.py in full and traced check_comparison (lines 84-114) by hand against every fixture in its test file. Normal form (actor.role == "member"): left is ast.Attribute attr='role' -> is_role_value True, comparator is a str Constant -> flagged; describe() (lines 73-81) renders "actor.role == 'member'", matching the assertion at test line 113. Reversed operands ("member" == actor.role): the elif at line 98 catches it and normalizes the message to role-side-first, matching test line 134. Plain variable (role == "member"): ast.Name with id in ROLE_NAMES (line 57) -> flagged, message "role == 'member'", matching test line 139. Inequality (actor.role != "admin"): ast.NotEq passes the line 90 filter and the symbol at line 102 renders "!=", matching test line 125. Membership forms (not in NAME, not in ("approver","admin")) are neither Eq nor NotEq, so line 90-92 skips them -- the fixed module and app/repositories/approvals.py-style code pass. Reading actor.role inside an f-string is a FormattedValue, not a Compare, so the real fixed module at user_admin_service.py:41 is not flagged; that is genuinely tested (test_role_mentioned_only_inside_a_message_is_accepted). False-positive risk against real code: I grepped all of app/ for role comparisons and found no "==" or "!=" against a role in any production module -- only assertEquals in test files, which the runner excludes via the test_ prefix filter (run-factory-checks.py:272), so the claimed "[OK] service-role-guard" for all three service modules is consistent with the actual file contents. Runner wiring verified: SERVICE_ROLE_GUARD (line 62), run_service_role_checks (lines 248-294), called at line 329 and ANDed into the result at line 331 with its report appended at line 332 -- the check cannot pass silently. Its directory-before-script ordering (lines 264-271) is not a loophole in practice and is required: /Users/clauskopp/Downloads/procurement-factory-demo/.claude/hooks/test_stop_validate_findings.py:96-108 copies only four guard scripts and creates no app/services/, so with the reverse order those hook tests would fail. Where service files exist, a missing guard script is still a hard failure (line 269). The near-duplication of run_service_sql_checks is stylistic, matches the file's one-function-per-check convention, and introduces no behavioural divergence I could find (both use the same test_-prefix filter and the same reporting shape); I do not consider it a defect. Guard test count (10) and runner test count (23 = 19 pre-existing + 4 new at lines 348-377) match the build order's claims exactly. I could not execute the guard; the traversal analysis above is static.
Scope Checked: The changed set (app/services/user_admin_service.py, app/services/test_user_admin_role_escalation_regression.py, factory/guards/validate-service-role-authorization.py, factory/guards/test_validate_service_role_authorization.py, factory/guards/run-factory-checks.py, factory/guards/test_run_factory_checks.py, plus the finding and build order themselves) is exactly what factory/build-orders/P1-DEMO-5.md:66-74 allows; modification-time ordering from Glob over factory/** and app/** is consistent with that set and shows no other recently touched file. None of the forbidden files were touched: app/repositories/users.py still has update_role unchanged at lines 55-63, app/repositories/approvals.py:43 is untouched, app/db.py, app/services/reporting_service.py, .github/workflows/, CLAUDE.md and .claude/ (hooks/skills/agents/settings) show no sign of modification. The runtime edit is confined to the authorization condition plus one module constant, as required. Two formal notes: (a) the Scope section does not itself list factory/findings/P1-DEMO-5.md and factory/build-orders/P1-DEMO-5.md, whose modification is inherent to the workflow and not forbidden -- I do not treat this as a violation; (b) without Bash I could not run git diff, so scope conformity rests on file content plus mtime ordering, not on the diff itself.
Remaining Risks: (1) The guard's rule is equality/inequality against a string LITERAL only, so an exclusion decision written as a membership test with the wrong content -- "if actor.role in DENIED_ROLES: raise" or "if actor.role in ('member',): raise" -- or against a named constant ("if actor.role == MEMBER_ROLE") would pass it. The guard docstring's "Deliberately NOT attempted" section (lines 35-46) covers the constant case under "does not follow values" and the wrong-content case under "cannot know whether the allowlist contains the right roles", but neither is named explicitly, and the finding's Central Guard Plan phrasing "makes the exclusion-style authorization test unwritable in the service layer" is broader than what the implementation actually enforces. (2) The guard covers only app/services/ (non-recursive); the same class of decision also lives in app/repositories/approvals.py:43, where an exclusion-style rewrite would go undetected. (3) /Users/clauskopp/Downloads/procurement-factory-demo/app/repositories/users.py:55 update_role remains unauthenticated, so any future caller that bypasses the service reaches the write with no role check at all -- deliberate per the build order (storage layer), but it means the boundary is a single service function rather than a choke point, unlike the approvals path where authorization sits in the repository. (4) Nothing pins the deliberately-omitted self-change behaviour: no test asserts that an admin may still demote themselves, so that decision could be silently reversed later; a related availability edge (last admin demotes themselves and no one can administer roles) is out of scope but real. (5) The quoted red/green outputs, the 76/68/23/10 test counts and the in-flight CI run cannot be confirmed with read-only tools -- they are internally consistent with the files, but only a live run proves them.
Findings And Objections: None that block closure. The two items I would have raised in an earlier round are recorded above as risks rather than defects: the finding's "unwritable in the service layer" claim overstates the guard's actual rule (literal-only == / !=) even though the same paragraph states the rule precisely and the guard's own limits section is otherwise honest; and the failure-case assertions use assertRaises rather than assertRaisesRegex, which I verified is safe for these specific inputs but is weaker than it could be. No test, assertion or control was weakened, skipped or deleted: test_user_admin_service.py is intact, test_run_factory_checks.py was only extended (all 19 pre-existing cases still present verbatim), no suppressions or disabled checks appear in the changed files, and no new import, parameter or private-attribute exposure was added to the runtime module. Note for the closing agent: the finding currently carries no CI Evidence or Review Artifact field, both of which validate-finding.py requires before READY_FOR_CLOSURE.
