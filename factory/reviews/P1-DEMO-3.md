# P1-DEMO-3

Finding: P1-DEMO-3
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: a39a0bae3dca8e1c8
Reviewed Commit: af3d00163f05c5f915ee6c57bba1d9db19351dd3 on branch fix/P1-DEMO-3 (base main was 100bc93f82937d2487d7202506851b86b2db535b, PR #7 at https://github.com/Pransanth/procurement-factory-demo/pull/7)
Result: PASS
Root Cause Addressed: Yes. The finding's Analyse states record_decision() validates org membership of the request and approver but never checks approver.role or self-approval. app/repositories/approvals.py:43-52 now adds exactly these two checks after the existing org-membership checks (lines 33-42) and before the INSERT (lines 55-60): `if approver.role not in ("approver", "admin"): raise ValueError(...)` and `if approver_user_id == request.requested_by_user_id: raise ValueError(...)`. This is the same location and mechanism the build order specifies (factory/build-orders/P1-DEMO-3.md lines 10-18). Confirmed via Grep that record_decision() is the sole `INSERT INTO approvals` site and app/services/procurement_service.py:54 is its only production caller, so this is a genuine single choke point, not a partial patch.
Regression Evidence Checked: Read app/repositories/test_approval_role_scope_regression.py in full. Both tests (test_member_role_cannot_approve_a_request, test_requester_cannot_approve_their_own_request) use `with self.assertRaises(ValueError): approvals.record_decision(...)`, which matches the build order's Red Regression Evidence quote ("AssertionError: ValueError not raised" for both, no import/syntax failure) and Green Runtime Fix Evidence quote (2 tests OK, unchanged assertions) exactly — nothing in the assertions has been loosened relative to what the build order documents. I traced the self-approval test's construction (approver = requester, role "approver") and confirmed it correctly isolates the self-approval branch (role check would pass, only the self-id check fires). I could not execute the tests myself (no Bash tool available) to independently reproduce red-then-green; this is a stated limitation, not a finding of fabrication — the code and quoted evidence are internally consistent.
Guard Evidence Checked: Read factory/guards/run-app-tests.py in full: discover_test_modules() (lines 27-32) does `APP_DIR.rglob("test_*.py")`, genuinely auto-discovering the new test file with no manual registration. Read .github/workflows/factory-ci.yml: lines 34-35 run "python3 factory/guards/run-app-tests.py" as its own explicit CI step on every push/PR to main (lines 7-11), separate from the "Run canonical factory checks" step (line 31-32, which per factory/guards/run-factory-checks.py lines 9-26 only validates findings/job-handler-scope/reviews, not app tests). This confirms the Central Guard Plan's claimed mechanism is real and was already independently verified for the identical P1-DEMO-2 claim (factory/reviews/P1-DEMO-2.md line 11); re-verifying it here rather than assuming it holds was appropriate and it does hold.
Scope Checked: Changed files exactly match the build order's Scope section (factory/build-orders/P1-DEMO-3.md lines 44-61): app/repositories/approvals.py (two new checks in record_decision(), no new import — confirmed the diff adds no import lines, consistent with the claim that `role` and `requested_by_user_id` were already available on the existing approver/request objects) and the new app/repositories/test_approval_role_scope_regression.py. Confirmed no changes to app/db.py, app/repositories/procurement_requests.py, or app/jobs/*, matching the explicit out-of-scope list. Confirmed P1-DEMO-2 (supplier_id validation in procurement_requests.create()) and this finding (role/self-approval validation in approvals.py:record_decision()) touch disjoint functions/files with no overlap, consistent with the finding's own non-overlap claim (factory/findings/P1-DEMO-3.md lines 42-45, 50).
Remaining Risks: None identified beyond the inherent limitation that this review is static/read-only and cannot itself execute the regression suite or independently hit the GitHub API to confirm the cited CI run (https://github.com/Pransanth/procurement-factory-demo/actions/runs/31802966670/job/94775056287) is genuine and green — the quoted evidence is internally consistent with the current source but was not independently re-run. Minor non-defect observation: the role check runs before the self-approval check, so a self-approving 'member' surfaces a role-authorization ValueError rather than a self-approval ValueError; this does not create a bypass since both paths still reject the action, and the self-approval test deliberately uses an authorized 'approver' role to isolate and prove the second branch independently.
Findings And Objections: None.
