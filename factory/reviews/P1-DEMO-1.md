# P1-DEMO-1

Finding: P1-DEMO-1
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: a3ff16aaa04ab729e
Reviewed Commit: c085b68603f12c6d506d7d2f3f21bf59dd82a5da
Result: PASS
Root Cause Addressed: Yes. The finding's Root Cause (factory/findings/P1-DEMO-1.md line 15) is that org scoping was enforced only by developer convention — a handler received a raw sqlite3.Connection plus a JSON payload and had to manually pass the right organization_id to every repository call, with nothing enforcing that it matched the job's own org. I read the actual fix: app/jobs/scoped_repositories.py:29-63 defines ScopedRepositories, permanently bound to one organization_id at __init__ (line 32-34); none of its six methods (list_pending_procurement_requests_older_than, update_procurement_request_status, record_audit_log, list_audit_log_older_than, archive_audit_log_entries, delete_audit_log_ids) accept an organization_id argument — I verified each signature directly. app/jobs/queue.py:82 constructs this object per-job inside run_pending() from `row["organization_id"]` (the queue's own authoritative DB column, set at enqueue time by queue.py:47-55), not from the job's payload; the module docstring (queue.py:1-17) explicitly states the payload copy of organization_id is "NOT used for authorization". Handlers are now called as `handler(scope, payload)` (queue.py:96), and both real handlers (app/jobs/approval_reminder.py:28-43, app/jobs/audit_log_archival.py:27-37) were migrated to this signature with no remaining app.repositories import. This closes the structural gap described in the Root Cause: a handler has no parameter through which it can name a different org for a tenant-scoped call.
Regression Evidence Checked: Read app/jobs/test_org_scope_regression.py in full and cross-checked its current assertion text against both the build order's quoted "red" failure output (factory/build-orders/P1-DEMO-1.md lines 154-172) and quoted "green" pass output (lines 227-237). The security assertion and its message ("SECURITY: a background job enqueued for Organization A was able to change Organization B's procurement request status from 'submitted' to '...'" — test file lines 109-118) are byte-for-byte consistent with what the build order quotes as having failed before the fix and passed after; only the buggy in-test handler's call signature changed from (conn, payload) to (scope, payload), which the build order discloses explicitly (lines 217-219) and which is a mechanical signature adaptation, not a weakening of the assertion. I also confirmed procurement_requests.update_status and get_by_id (app/repositories/procurement_requests.py:58-63,74-82) filter by organization_id in the SQL WHERE clause, which is why a scope bound to Org A silently no-ops against Org B's row (0 rows affected) rather than erroring — matching the documented mechanism. I could not independently execute the test suite (no Bash tool available); I relied on reading the test file, the production code it exercises, and the build order's quoted transcripts, which are internally consistent with the current on-disk state.
Guard Evidence Checked: Read factory/guards/validate-job-handler-scope.py end-to-end. It correctly identifies "production handler" files via a module-level register() call (find_module_level_register_call, lines 82-88), and checks (1) direct app.repositories imports (check_tenant_repository_imports, lines 98-117), (2) blocked parameter names {conn, connection, organization_id, org_id} on the registered handler function (check_handler_signature, lines 120-140), (3) any `_conn` attribute access anywhere in the file (check_private_scope_attribute_access, lines 143-152) — all three match what the build order's "Central Guard Evidence" section claims it detects. Cross-checked against factory/guards/test_validate_job_handler_scope.py, which exercises all three rules plus a combined-violations case against inline fixture strings (not real files under app/jobs/) and a not-a-handler skip case; the fixtures' expected failure substrings ("app.repositories", "'conn'", "'organization_id'", "_conn") match the guard's actual error message text. scoped_repositories.py itself correctly never triggers as a "handler" (no register() call), consistent with the documented exemption. run-factory-checks.py (lines 93-134) wires this guard over every non-test *.py file under app/jobs/, and .github/workflows/factory-ci.yml step "Run canonical factory checks" (line 31-32) invokes exactly that runner, so the guard is enforced in CI going forward for any future handler file.
Scope Checked: The finding-specific changes (app/jobs/queue.py, app/jobs/handlers.py, app/jobs/scoped_repositories.py [new], app/jobs/approval_reminder.py, app/jobs/audit_log_archival.py, their test files, app/jobs/test_org_scope_regression.py [new], factory/guards/validate-job-handler-scope.py [new] + its test, factory/findings/P1-DEMO-1.md, factory/build-orders/P1-DEMO-1.md) match build-orders/P1-DEMO-1.md's "Scope" section (lines 91-107: changes confined to app/jobs/ plus a new guard script under factory/guards/, plus matching test files) exactly — no schema change, no touch to app/services/procurement_service.py or the interactive repository API, as required. The remaining changed files in the diff (.claude/agents/finding-closure-reviewer.md, .claude/hooks/stop-validate-findings.py, .claude/hooks/subagentstop-write-review.py + its tests, .claude/skills/verify-finding/SKILL.md, factory/guards/validate-finding.py, factory/guards/validate-review.py + tests, factory/reviews/README.md, factory/README.md, .claude/settings.json, .github/workflows/factory-ci.yml) are generic factory-workflow tooling; I checked their content directly (e.g. SKILL.md's only P1-DEMO-1 reference is a generic usage example, finding-closure-reviewer.md has none) and found nothing P1-DEMO-1-specific or job/org-scope-specific in them — consistent with being separate, pre-existing infrastructure rather than part of this finding's fix, as the reviewing prompt characterized them and as I was able to independently corroborate from the files' own content.
Remaining Risks: (1) .github/workflows/factory-ci.yml only runs run-factory-checks.py (finding structural validation + job-handler AST guard + review structural validation) and separate unittest invocations for factory guard/hook tests — it never executes app/jobs/test_org_scope_regression.py, app/test_multi_tenant_isolation.py, or any of the other ~53 app-level tests. This is honestly documented in factory/README.md ("Die Prüfkette: lokal bis CI" and the Stop-hook section, which also notes required-status-check branch protection is "noch nicht konfiguriert"), and is outside this build order's stated Acceptance Criteria (#6 only requires the app suite to be green locally, not in CI), so I do not treat it as a defect in this fix. But it is a genuine forward-looking gap: if a future change to scoped_repositories.py itself (which the AST guard explicitly does not check, since it never calls register()) reintroduced an organization_id parameter or similar bypass, neither CI nor branch protection would catch it — only local test discipline would. (2) The build order's own "Primäre Sicherheitsgrenze" section (factory/build-orders/P1-DEMO-1.md lines 15-17) states ScopedRepositories is constructed "aus payload[\"organization_id\"]", which is inconsistent with both the actual code (queue.py:82, constructed from `row["organization_id"]`, the queue's own trusted column) and the build order's own later "Green Runtime Fix Evidence" section (lines 200-205), which correctly describes the row-based, non-payload construction. This is a stale/inconsistent planning sentence in the build order, not a code defect — the actual implementation is the more secure of the two described designs — but it is worth correcting for future readers.
Findings And Objections: None that block closure. The two items above are disclosed as Remaining Risks rather than objections because they are pre-existing/documentation issues outside this fix's own correctness, not evidence of a weakened test, a misrepresented result, or an unaddressed root cause. I was not able to execute any code (no Bash tool); all verification is from reading the current source, test files, and the build order's quoted transcripts, cross-checked for internal consistency rather than independently re-run.
