# P1-DEMO-4

Finding: P1-DEMO-4
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: adba3cafb843eb19c
Reviewed Commit: 32563f7 (branch fix/P1-DEMO-4, PR #13 against main; branch base is origin/main 4d0ea760734cb7dd0d1b656043a1b50eb580e092)
Result: PASS
Root Cause Addressed: Yes, at the documented place and nowhere adjacent. The Root Cause (finding line 59: the line-item query carries no organization_id predicate, does not bind organization_id, and its suppliers join is untenanted) is fixed in /Users/clauskopp/Downloads/procurement-factory-demo/app/services/reporting_service.py:36-47 — "JOIN suppliers s ON s.id = pr.supplier_id AND s.organization_id = pr.organization_id" (lines 41-42) and "WHERE pr.organization_id = ? AND pr.status = 'approved' AND pr.created_at LIKE ?" (line 43), with binds (organization_id, month_prefix, top_line_item_limit) (line 46); placeholder order matches bind order (WHERE org, LIKE month, LIMIT n) and the JOIN condition holds no placeholder. The already-correct totals query (lines 29-34) is byte-unchanged, the signature (lines 17-19) and return shape (lines 49-64) are unchanged, as the build order's Scope demands. The guard remains explicitly the second layer, not the boundary (guard docstring lines 7-12, 75-76). Grep confirms conn.execute exists only in app/services/reporting_service.py:29 and :36 — no other hand-written service SQL exists to be missed.
Regression Evidence Checked: I read /Users/clauskopp/Downloads/procurement-factory-demo/app/services/test_reporting_org_scope_regression.py in full and cross-checked it against "Red Regression Evidence" (build order lines 108-126). The current assertions reproduce the quoted failures exactly and are unchanged since round 2: test_top_line_items_contain_no_foreign_organizations_data asserts assertEqual(descriptions, ["A laptops"]) (lines 60-62) and additionally pins supplier_name == ["A Supplies"] (lines 63-65), which pre-fix yields "['B confidential tooling', 'A laptops'] != ['A laptops']" because 900_000 > 10_000; test_foreign_line_items_do_not_displace_own_items_from_the_top_list uses top_line_item_limit=2 (line 83) and asserts ["A laptops", "A chairs"] (lines 86-89), pre-fix "['B confidential tooling', 'A laptops'] != ['A laptops', 'A chairs']"; test_headline_totals_stay_scoped asserts count 1 / total 10_000 (lines 105-106), consistent with being green pre-fix since the totals query was already scoped. All three are strict exact-list/exact-value equality — no loosening, no removed case, no softened message versus what the build order documents. The baseline /Users/clauskopp/Downloads/procurement-factory-demo/app/services/test_reporting_service.py is intact (4 single-tenant tests, lines 48-100, docstring lines 8-11 still reserving the adversarial test for P1-DEMO-4) and its single-tenant results are unaffected by an additive predicate. The regression file is auto-discovered by factory/guards/run-app-tests.py (rglob, lines 27-32), so it does run in CI's "Run application test suite". LIMITATION: read-only, no Bash — "2 red then 3 green" and the 3/10/72 counts remain claims that are internally consistent with the file content and the SQL, but only a live run confirms them.
Guard Evidence Checked: I read factory/guards/validate-service-sql-org-scope.py and factory/guards/test_validate_service_sql_org_scope.py in full and hand-traced all 22 fixtures. The round-2 fail-open is genuinely closed: INSERT_CONFLICT_CLAUSE (line 151) is applied to BOTH INSERT_TARGET_RE (153-156) and INSERT_COLUMNS_RE (158-161), and since both share the byte-identical prefix through the table name, their match.start() keys in columns_by_position (line 225, lookup line 233) necessarily align — including for the conflict spellings. Traced: INSERT_OR_REPLACE_WITHOUT_COLUMN_LIST -> columns regex cannot match ("VALUES" blocks \s*\(, and shortening the table-name atom never produces "("), column_text is None -> "keine explizite Spaltenliste", exit 1 (test asserts "Spaltenliste"); INSERT_OR_IGNORE_WITHOUT_ORG_COLUMN -> columns "name, created_at" -> organization_id-missing error, exit 1; INSERT_OR_REPLACE_WITH_ORG_COLUMN -> columns contain organization_id, no SELECT, early return [] -> exit 0. No new false negative found: check_insert_targets() is only reached when is_insert (line 256-259), so the read path is untouched and the widening can only add checks; no new false positive found (the added alternatives are the complete SQLite conflict set). The round-1 fixes still hold: comma lists via FROM_LIST_TAIL_RE/FROM_LIST_ENTRY_RE (137-145, 188-199) reject COMMA_JOIN_UNSCOPED twice and COMMA_JOIN_PARTIALLY_SCOPED exactly once (the assertNotIn("Tabelle 'procurement_requests'") holds because that name appears only inside the echoed statement); INSERT ... SELECT sources are checked (264-268). TENANT_TABLES (90-98) matches app/db.py exactly (users, suppliers, procurement_requests, approvals, audit_log, audit_log_archive, jobs; organizations correctly excluded — db.py lines 18-91). The docstring's "Deliberately NOT attempted" block (49-74) now names exactly the three limits round 2 reported — whole-literal/per-branch predicate matching, unverified right-hand "<other>.organization_id", REPLACE INTO landing in the read path (fails closed) — plus dynamic SQL, presence-not-value and unparsed subqueries/CTEs; I found no remaining claim in it that the code does not honour. Counts verified by hand: 22 test methods in the guard test file (12 + 7 + 3), 19 in test_run_factory_checks.py (14 pre-existing + 5 service cases, lines 292-326); nothing deleted, skipped or loosened. Runner wiring (run-factory-checks.py:187-237, 268-274) is purely additive, and the dir-before-script ordering (206-212) is not a weakening — with app/services/ present a missing guard script is still a hard [FEHLER]; .claude/hooks/test_stop_validate_findings.py contains no reference to services/the new guard, i.e. it was not adapted.
Scope Checked: Respected. Build order Scope (lines 60-68) allows: the line-item query in app/services/reporting_service.py, the new file app/services/test_reporting_org_scope_regression.py, the new guard plus its test file, and the run-factory-checks.py wiring plus test_run_factory_checks.py. The changed set I was given for 32563f7 (guard, guard tests, build order, finding) plus the earlier commits' set matches that; finding and build order are the workflow's own artifacts and factory/reviews/P1-DEMO-4.md is written by the SubagentStop hook, not by the implementing agent. Forbidden paths are demonstrably untouched: .github/workflows/factory-ci.yml still lists the same five steps and still does NOT name factory.guards.test_validate_service_sql_org_scope (line 38), conn.execute occurs only in reporting_service.py, and the Stop-hook test file has no service references. LIMITATION: without Bash I cannot run git diff, so "only these files changed" rests on the diffstat I was given plus content inspection.
Remaining Risks: (1) CI Evidence does not yet cover the reviewed commit: findings/P1-DEMO-4.md line 68 records green runs 31809137281 (c1ab345) and 31809842106 (e078606), but the guard source changed again in 32563f7; validate-finding.py only checks that the field is non-empty (fields list line 97), so nothing mechanical prevents closing on superseded evidence. (2) The guard's own 22 unit tests still run only locally — CI exercises the new rule solely through the 5 runner cases and run-factory-checks.py on real files, so the three conflict-clause cases added here have no CI coverage. (3) run_service_sql_checks uses services_dir.glob("*.py") (run-factory-checks.py:214), i.e. non-recursive, while that script's docstring says "Every file under app/services/" (lines 25-30): a future app/services/<subpackage>/foo.py with raw SQL would be silently unchecked. Same shape as the pre-existing jobs check; no subdirectory exists today. (4) iter_sql_literals only inspects the first POSITIONAL argument (lines 305-310), so conn.execute(sql=...) is not seen at all — not even counted as "nicht analysierbar". (5) Dynamic SQL (f-string/concatenation) exits 0 with only an informational suffix, and run-factory-checks.py forwards guard stdout only on failure (226-235), so that honesty caveat is invisible at runner level. (6) Read-only limitation: I executed nothing — red/green transitions, the 22/19/72/10 counts, exit codes and the Actions run statuses are cross-checked for internal consistency against file content only, never reproduced.
Findings And Objections: (a) Hard precondition, not a defect in this commit: before READY_FOR_CLOSURE the finding's CI Evidence must be updated to a green Actions run for the actual closure commit (32563f7 or its successor); as it stands the recorded CI evidence covers code that differs from what I reviewed. The finding itself acknowledges this (line 68, last sentence), which is honest but not yet satisfied. (b) Minor documentation overclaim outside the changed guard, worth fixing when that file is next in scope: run-factory-checks.py's docstring says "Every file under app/services/" while the implementation globs only the top level (line 214). (c) I could execute nothing; every quoted command output in the build order and finding is cross-checked against file content only, so the claim "22 tests -> OK" and the green-after-fix claim are consistent-but-unreproduced by me and would only be settled by the in-flight CI run.
