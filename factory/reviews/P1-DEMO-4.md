# P1-DEMO-4

Finding: P1-DEMO-4
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: a9fa593e034e6950c
Reviewed Commit: c1ab345a09f0002f38713a86d75c6fd45bcec6ec (branch fix/P1-DEMO-4, PR #13 against main; branch base is origin/main 4d0ea760734cb7dd0d1b656043a1b50eb580e092)
Result: PASS
Root Cause Addressed: Yes. The documented Root Cause (finding line 59: the second query in monthly_spend_report carries no organization_id predicate and does not bind organization_id; the suppliers join is likewise untenanted) is addressed at exactly that place: /Users/clauskopp/Downloads/procurement-factory-demo/app/services/reporting_service.py:41-46 now reads "JOIN suppliers s ON s.id = pr.supplier_id AND s.organization_id = pr.organization_id" and "WHERE pr.organization_id = ? AND pr.status = 'approved' AND pr.created_at LIKE ?" with binds (organization_id, month_prefix, top_line_item_limit) -- placeholder order and bind order match (WHERE org, LIKE month, LIMIT n), and the JOIN condition contains no placeholder. The already-correct totals query (lines 29-34) is byte-identical to before, as the build order's Scope demanded. No signature or return-shape change (lines 17-19, 49-64). Nothing adjacent was substituted for the real fix.
Regression Evidence Checked: I read /Users/clauskopp/Downloads/procurement-factory-demo/app/services/test_reporting_org_scope_regression.py in full and cross-checked it against the build order's "Red Regression Evidence" (lines 108-126). The quoted failures reproduce exactly from the assertions as they stand today: test_top_line_items_contain_no_foreign_organizations_data asserts assertEqual(descriptions, ["A laptops"]) (lines 60-62) and, pre-fix, 900_000 > 10_000 puts "B confidential tooling" first -> exactly "['B confidential tooling', 'A laptops'] != ['A laptops']"; test_foreign_line_items_do_not_displace_own_items uses top_line_item_limit=2 (line 83) and asserts ["A laptops", "A chairs"] (lines 86-89) -> exactly "['B confidential tooling', 'A laptops'] != ['A laptops', 'A chairs']". The third test (lines 91-106) asserts count 1 / total 10_000 and is consistent with the claim that it was already green pre-fix, since the totals query was already scoped. Assertions are strict exact-list equality and additionally pin supplier_name (lines 63-65), i.e. stronger, not weaker, than the minimum; there is no sign of any post-red loosening. Repository signatures used by the test match app/repositories (organizations.create, users.create, suppliers.create, procurement_requests.create/update_status with now=), so the red failures were genuine AssertionErrors, not import/signature errors. The file is auto-discovered by factory/guards/run-app-tests.py (rglob "test_*.py" over app/, lines 27-32), so it runs in CI's "Run application test suite" step. LIMITATION: I have read-only tools and no Bash; I could not execute unittest, so "2 red then 3 green", the 10/72 test counts and the exit codes remain claims -- they are internally consistent with the file contents and the SQL, but only a live run confirms them.
Guard Evidence Checked: I read /Users/clauskopp/Downloads/procurement-factory-demo/factory/guards/validate-service-sql-org-scope.py and traced both pinned fixtures by hand. UNFIXED_REPORTING_QUERY (test file lines 22-37): TABLE_REF_RE yields [(procurement_requests, pr), (suppliers, s)], neither has_predicate_for("pr") nor ("s") matches, single_unaliased_table is False -> two errors, exit 1, stderr naming both tables -- matching test_unfixed_reporting_query_is_rejected (lines 144-149). FIXED_REPORTING_QUERY (lines 39-55): "pr.organization_id = ?" satisfies pr, "s.organization_id = pr.organization_id" satisfies s via the other-table-equality RHS alternative (line 114) -> exit 0. Both fixtures are byte-equivalent to the real pre-/post-fix query. TENANT_TABLES (lines 64-72) matches app/db.py exactly (users, suppliers, procurement_requests, approvals, audit_log, audit_log_archive, jobs; organizations correctly excluded). The runner wiring in factory/guards/run-factory-checks.py:187-237 and 240-274 is purely additive (fourth check, --services-dir default app/services, test_*.py excluded, failure aggregated into the overall exit code). On the ordering question I was explicitly asked to judge: putting "does services_dir exist?" (line 206) before "does the guard script exist?" (line 210) is NOT a weakening -- wherever app/services/ exists, a missing guard script is still a hard [FEHLER]/False (lines 210-212); only the "no service layer at all" case is skipped, which is the same tolerance the three pre-existing checks already grant for their own missing directories (lines 72-74, 115-117, 158-160). I confirmed the stated cause: .claude/hooks/test_stop_validate_findings.py copies only validate-finding.py, run-factory-checks.py, validate-job-handler-scope.py and validate-review.py into throwaway projects (lines 100-108) and creates no app/services/, so the guard-script-first order would have failed a project with nothing to check. That hook test file itself is unmodified. Two of the five new runner cases (test_scoped_service_sql_passes, test_unscoped_service_sql_fails) run in GitHub CI via .github/workflows/factory-ci.yml line 38, so the new rule is genuinely exercised in CI without touching the workflow.
Scope Checked: Respected. Build order Scope (lines 60-68) allows: the line-item query in app/services/reporting_service.py, the new app/services/test_reporting_org_scope_regression.py, the new factory/guards/validate-service-sql-org-scope.py plus its test file, and the wiring into factory/guards/run-factory-checks.py plus test_run_factory_checks.py. The reported changed set is exactly that plus the finding and its own build order (the workflow's own artifacts). Nothing forbidden was touched: app/repositories/*, app/jobs/*, app/db.py, app/services/procurement_service.py, app/services/user_admin_service.py, app/services/test_reporting_service.py, .github/workflows/factory-ci.yml, CLAUDE.md, hooks/permissions all show as unchanged in the given diffstat, and grep confirms reporting_service.py is the only file under app/services/ containing conn.execute at all. On the "were tests weakened" question for factory/guards/test_run_factory_checks.py: I could not see the diff, but I reconstructed the pre-image count from prior evidence -- P1-DEMO-1's build order records validate_finding+run_factory_checks+job_handler_scope = 22 with job_handler_scope = 7 and test_validate_finding then at its original 6 cases (test_validate_finding.py lines 198-228; lines 287-336 are the later review/closure additions), leaving 9 runner cases = the 4 findings + 5 jobs cases still present verbatim today; the 5 review cases were added later (14), and the 5 service cases are new (19). No pre-existing case is missing and none of the retained assertions is loosened (all still assert returncode and exact [OK]/[FEHLER] lines). The build order's "19 tests -> OK (15 bestehende + 4 neue)" is therefore a prose miscount (actually 14 + 5); the headline total matches the file.
Remaining Risks: (1) Guard detection gap not listed under "Bewusste Grenzen": TABLE_REF_RE (validate-service-sql-org-scope.py:102-106) only recognizes tables introduced by from/join/update/into, so a comma-separated FROM list ("FROM procurement_requests pr, suppliers s WHERE pr.organization_id = ?") would leave suppliers undetected and pass -- a future hand-written query in that style could reintroduce exactly this finding's class of leak without the guard objecting. (2) INSERT handling is narrower than documented: check_statement returns early for any statement starting with INSERT (line 170), so "INSERT INTO tenant_table SELECT ... FROM procurement_requests" is never scope-checked on its SELECT source, and INSERT_INTO_RE (lines 108-110) requires an explicit column list, so "INSERT INTO suppliers VALUES (...)" is silently accepted. (3) The guard can only see the predicate text, never the value bound to "?" -- a query with pr.organization_id = ? bound to a caller-supplied/foreign org id passes; the primary boundary remains the caller convention, which the guard docstring does state. (4) Non-literal SQL is counted as "nicht analysierbar" by the guard, but run-factory-checks.py only forwards guard stdout on failure (lines 226-235), so a service file whose SQL is entirely non-literal is reported as a bare "[OK] service-sql-guard: x.py" and the honesty caveat is lost at runner level; no current service file is affected. (5) factory.guards.test_validate_service_sql_org_scope is deliberately not added to the CI workflow (scope decision, build order lines 77-80), so the guard's 12 edge-case rules have no CI coverage -- only the scoped/unscoped pair via test_run_factory_checks does. (6) Read-only limitation: all quoted command outputs, exit codes and test counts, and the CI status of PR #13, could not be re-executed or queried by me; the finding as it stands (Status VERIFYING) contains no CI Evidence or Review Artifact field yet, which validate-finding.py will structurally require before READY_FOR_CLOSURE.
Findings And Objections: Only the following, none of which contradicts the closure claim: (a) the build order's Central Guard Evidence line "19 tests -> OK (15 bestehende + 4 neue)" misstates the split -- the file contains 14 pre-existing plus 5 new cases (the fifth, test_invalid_finding_and_unscoped_service_sql_are_both_reported, is not named in the enumeration); the total is correct and I found no deleted or loosened pre-existing case; (b) the guard's documented rule set ("Tabellen aus FROM/JOIN/UPDATE/INSERT INTO/DELETE FROM", build order line 91) is broader than its implementation, which misses comma-join FROM lists and does not scope-check INSERT ... SELECT sources or column-list-less INSERTs -- these belong in the guard's "Bewusste Grenzen" section but are not there; (c) I could not execute anything, so the red-then-green transition, the 72-test app run, and the CI run for commit c1ab345 are cross-checked only for internal consistency against file content, not independently reproduced.
